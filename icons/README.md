Build-time only. `build.py` inlines these into the HTML, so nothing here is
published as a separate file and the artwork follows `currentColor`.

- `dark_mode.svg`, `light_mode.svg` — Feather Icons (`moon`, `sun`),
  MIT License, Copyright (c) 2013-2023 Cole Bemis.
  https://github.com/feathericons/feather
- `bluesky.svg`, `github.svg` — the respective services' marks, used to link
  to those services.
- `aboutme.svg` — pre-existing site asset.

All were edited to use `fill="currentColor"` / `stroke="currentColor"`.
